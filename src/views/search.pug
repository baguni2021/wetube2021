extends base
include mixins/video

block content
    form(method="GET").seach__form
        input(name="keyword", placeholder="Search by title",type="text" required)
        button 
            i.fas.fa-search

    div.video-grid
        each video in videos
            +video(video)
        else
            span.empty__message No videos found 
